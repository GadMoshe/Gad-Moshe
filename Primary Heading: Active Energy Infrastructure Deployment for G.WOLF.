# G.WOLF_PROTOCOL: ENERGY_CORE_MODULE
# STATUS: INITIALIZING_POWER_GRID
def distribute_energy(system_load):
    power_grid = {"base": 0.32, "peak": 0.640}
    return {module: system_load * factor for module, factor in power_grid.items()}
